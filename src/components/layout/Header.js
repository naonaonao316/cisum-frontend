import React, { Component } from 'react';

const Header = (props) => (
  <header>
    <h1>Cisum</h1>
    <nav>
      <ul>
        <li><a href="">Home</a></li>
      </ul>
    </nav>
  </header>
);

export default Header;
